<table class="form-table">
    <tr valign="top">
        <th scope="row">{{ 'User Disabled'|__ }}</th>
        <td>
            <fieldset>
                <legend class="screen-reader-text">
                    <span>{{ 'User Disabled'|__ }}</span>
                </legend>
                <label for="user_disabled">
                    <input type="hidden" name="adi_user_disabled" value="0"/>
                    <input type="checkbox" name="adi_user_disabled"
                           id="user_disabled" {{ userDisabled ? 'checked' : '' }} value="1"/>
                    {{ 'If selected, the user can not log in and his e-mail address will be changed for security reasons. The e-mail address is restored if the user is reenabled.'|__ }}
                </label>

                {% if userDisabled %}
                    <p class="description"> {{ 'Information on last disabling: '|__ ~ disabledReason }}</p>
                {% endif %}

                <p class="description">{{ 'Attention: This flag is automatically set (or unset) by Sync to WordPresss and its state may change on next run of synchronization.'|__ }}</p>
            </fieldset>
        </td>
    </tr>
</table>